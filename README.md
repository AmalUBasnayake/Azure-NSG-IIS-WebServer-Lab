# Azure-NSG-IIS-WebServer-Lab
Hands-on Azure lab: Create &amp; associate NSG to subnet, configure inbound HTTP rule (port 80), deploy Windows Server VM, install IIS using Run Command (PowerShell), and verify default IIS welcome page. AZ-500 aligned (platform protection + secure compute). Cybersecurity &amp; cloud networking portfolio project.
